<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link rel="stylesheet" href="/style.css"> <!-- <-- ADDED THIS LINE -->
</head>
<body>
  <div class="container"> <!-- Added container for styling -->
    <h2>Login</h2>
    <% if (message) { %>
      <p class="error-message"><%= message %></p> <!-- Used CSS class instead of inline style -->
    <% } %>
    <form method="POST" action="/login">
      <label for="username">Username:</label> <!-- Added label for accessibility & styling -->
      <input id="username" name="username" placeholder="Username" required/>
      <label for="password">Password:</label> <!-- Added label for accessibility & styling -->
      <input type="password" id="password" name="password" placeholder="Password" required/>
      <button type="submit">Login</button>
    </form>
    <p>Don't have an account? <a href="/signup">Sign Up</a></p> <!-- Changed text to match signup.ejs -->
  </div>
</body>
</html>
